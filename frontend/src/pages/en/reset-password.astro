---
import { routes } from '@/lib/routes'

// Redirect to canonical EN reset path
if (Astro.redirect) {
  return Astro.redirect(routes.en.resetPassword)
}
return Astro.redirect(routes.en.resetPassword)
---
